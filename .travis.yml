language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"

# Install packages
# Install packages
install:
  - pip install -r requirements.txt
  - pip install pytest-cov
  - pip install codecov
  - pip install .

